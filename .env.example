# HTTP port exposed on the host. Use 127.0.0.1:8787:8787 in docker-compose.yml
# if the dashboard should only be reachable from this machine.
DASHBOARD_PORT=8787
# Filled by install.sh from /var/run/docker.sock. Required for Docker metrics.
DOCKER_GID=999
